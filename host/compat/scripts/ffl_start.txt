# FF Legend input test: title is up by ~frame 62; the title screen only
# responds to A (not Start). A held for 60 frames leaves the title -
# broken input/interrupt delivery pins the unresponsive title hash
# instead. this was broken until the deadline scheduler fired LYC=0
# STAT events chronologically through the frame wrap (phase 2)
300:A
360:
